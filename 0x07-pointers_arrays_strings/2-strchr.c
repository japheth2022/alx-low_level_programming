#include "main.h"
#include <string.h>


/**
 * _strchr - character search function
 * @s: string
 * @c: character
 * Return: a string
 */







char *_strchr(char *s, char c)
{
	int a = 0, b;
	while (s[a])
	a++;
	{
		if (c === s[a])
		s += b;
		return (s);


	}
	return (NULL);
}

